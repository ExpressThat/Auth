# UI
All Ui Should be done using [DaisyUI](https://daisyui.com/llms.txt)
you should try and create reusable components in the ui package as possible








# Tools
remember when installing frameworks like daisyui that use tailwind v4 that it uses tailwind v4 which dose not use config files anymore and instead uses the global.css and things like `@plugin`


# Always run the following
Please always run a type check and run the format-and-lint command and fix any issues after you have completed all your tasks
