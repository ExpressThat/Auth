# Management Web

Status: planned application shell.

Owns the React customer and platform administration experience. It consumes
management contracts and presents explicit tenant and environment context for
sensitive actions; it never imports server repositories or secrets.

The Vite application supports Workers and Docker deployments. See the
[workspace register](../../docs/architecture/workspace-ownership.md).
