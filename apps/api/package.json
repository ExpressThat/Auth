{
  "name": "api",
  "version": "0.0.1",
  "private": true,
  "description": "ExpressThat Auth API – ASP.NET Core Web API",
  "scripts": {
    "build": "dotnet tool restore && dotnet publish Api.csproj --configuration Release --output publish",
    "dev": "dotnet tool restore && dotnet watch --no-hot-reload run --launch-profile Development",
    "check-types": "dotnet build",
    "start": "dotnet publish/Api.dll"
  },
  "dependencies": {
   
  }
}
