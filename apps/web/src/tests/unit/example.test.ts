import { describe, it } from "vitest";

describe("web unit tests", () => {
  it.todo("covers a real component render and its visible UI states");
  it.todo("covers a real helper or hook used by the web app");
});
