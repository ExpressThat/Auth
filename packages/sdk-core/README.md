# SDK Core

Status: planned package shell.

Owns shared foundations for generated API clients, including typed errors,
transport policy, retries, pagination, and version metadata.

Released SDKs derive from immutable OpenAPI contracts rather than monorepo
server types. See the [release process](../../docs/releases/release-process.md).
