# Database Conformance

Status: planned package shell.

Owns the shared repository, transaction, isolation, migration, pagination,
concurrency, and dialect-parity suites applied to every database adapter.

SQLite and PostgreSQL must satisfy the same observable contracts. See
[ADR-0022](../../docs/decisions/0022-pluggable-database-adapters.md).
