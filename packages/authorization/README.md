# Authorization

Status: planned package shell.

Owns permissions, roles, grants, conditions, and fail-closed policy evaluation.
It evaluates trusted tenant and environment context without depending on an
HTTP framework, provider, database dialect, or deployment target.

See the [trusted tenant context decision](../../docs/decisions/0012-trusted-tenant-context.md).
