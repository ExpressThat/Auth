# UI

Status: active foundation package.

Owns shared HeroUI components, accessible state patterns, and design tokens for
the two React applications. It never imports server repositories, runtime
secrets, or deployment bindings.

Use package-local type, unit, component, and coverage commands through pnpm
filters. See the [contributor guide](../../CONTRIBUTING.md).
